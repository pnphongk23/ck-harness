# Repository Agent Router

Start with [[docs/harness/index|Harness Index]]. Follow the workflow matching
the request under `docs/harness/workflows/`. Markdown under `docs/harness/` is
canonical. Never launch another agent from repository tooling.
