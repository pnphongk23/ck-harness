# Repository Agent Router

Start with the Harness Index (resolved from `index.md` under the Harness root, by default [[docs/harness/index|Harness Index]]). Follow the workflow matching the request under the resolved workflows folder (default: `docs/harness/workflows/`). Markdown under the resolved Harness root (default: `docs/harness/`) is canonical.
