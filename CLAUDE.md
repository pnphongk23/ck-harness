# Claude Adapter

Read the Harness Index (resolved from `index.md` under the Harness root, by default [[docs/harness/index|Harness Index]]), then use the relevant document in the resolved workflows folder (default: `docs/harness/workflows/`). Plans use ordered `work-item-XX-*.md` children; implementation requires verification evidence.
